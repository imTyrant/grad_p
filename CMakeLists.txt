#/CMakeLists.txt
PROJECT(grad_p)
CMAKE_MINIMUM_REQUIRED(VERSION 2.6) 
ADD_SUBDIRECTORY(ia_tpm)
AUX_SOURCE_DIRECTORY(. DIR_SRCS)
ADD_EXECUTABLE(grad_p ${DIR_SRCS}  )
TARGET_LINK_LIBRARIES( grad_p IA_TPM libtspi.so)